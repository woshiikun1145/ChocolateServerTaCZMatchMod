# Chocolate Server Tacz Match Mod（CSTMM）配置手册

面向服务器管理员与地图配置人员，说明如何使用配置界面与配置文件设置地图、商店与全局参数。

> 玩家操作见 [README.md](README.md) ｜ 运行原理与网络协议见 [DEVELOPER.md](DEVELOPER.md)

---

## 目录

1. [打开配置界面](#1-打开配置界面)
2. [配置一张地图](#2-配置一张地图)
3. [字段详解](#3-字段详解)
4. [商店物品配置](#4-商店物品配置)
5. [全局配置](#5-全局配置)
6. [配置文件直接编辑](#6-配置文件直接编辑)
7. [典型配置示例](#7-典型配置示例)
8. [注意事项](#8-注意事项)

---

## 1. 打开配置界面

- 游戏内执行命令：`/cstmm config`（OP≥2）

**配置界面只能通过该命令打开**，匹配菜单（; 键）不提供配置入口。

界面布局：左侧为地图列表（可滚动，含"添加地图 / 删除选中"按钮），右侧为当前地图的编辑区（可滚动），右上角"保存配置 / 重新加载"，右下角"返回"。

- **保存**：点击"保存配置"后，配置经校验发送到服务端并自动同步给所有在线玩家。
- **重载**：点击"重新加载"丢弃当前编辑内容，重新从服务端拉取最新配置。
- **保护机制**：
  - 有未保存修改时，被动收到的配置同步不会覆盖你的编辑（会提示已跳过自动刷新）。
  - 切换地图若未保存，会弹窗确认。
  - 保存前自动校验：地图 ID 重复、装备槽位非法时阻止保存并弹窗提示。

---

## 2. 配置一张地图

1. 左侧点击"+ 添加地图"，自动生成唯一 ID（如 `map_3`）。
2. 在右侧填写显示名称、选择结算方式、设置人数与对局参数。
3. 配置出生点（**必须至少各 1 个**，否则无法保存）与边界。
4. 需要商店时添加商品。
5. 点击"保存配置"。

---

## 3. 字段详解

### 3.1 基本设置

| 字段 | 说明 |
|---|---|
| **地图 ID** | 唯一标识，仅字母/数字/下划线/连字符。**不可重复**——ID 相同的地图保存时会互相覆盖 |
| **显示名称** | 玩家匹配卡片上显示的名字 |
| **启用/禁用** | 禁用的地图不出现在玩家匹配列表 |

### 3.2 结算方式

| 选项 | 含义 |
|---|---|
| `KILLS`（击杀） | 率先达到"目标击杀数"的队伍获胜 |
| `TIMER`（计时） | 倒计时"最大时长(秒)"结束时，击杀数多者获胜 |

**平局规则**（仅 TIMER 模式有效）：

| 选项 | 含义 |
|---|---|
| `OVERTIME` | 平局时向全体玩家发起加时投票，全票通过则延长对局 |
| `DRAW` | 平局时直接结束比赛 |

### 3.3 人数控制

| 字段 | 说明 |
|---|---|
| **红/蓝队最低人数** | 开局门槛：某模式队列总人数达到**两队下限之和**即开局（开局时才自动分配队伍：战队尽量同队、两队均衡）。同时也是 CONDITIONAL 补位模式的补位阈值 |
| **红/蓝队最高人数** | 该队允许的最大人数，**0 = 无上限**；两者之和也是该模式队列的容量上限（满员后拒绝入队）。开局分队、快速匹配拆队、补位都会遵守 |

### 3.4 对局参数

| 字段 | 说明 |
|---|---|
| **准备时间(秒)** | 开局后的倒计时阶段，期间不记分 |
| **边界警告(秒)** | 玩家离开地图边界后，超过该秒数未返回即被处决 |
| **边界惩罚(击杀)** | 越界被处决时，对方队伍获得的惩罚击杀数，本人记一次死亡 |
| **踢人冷却(秒)** | 同一玩家两次发起踢人投票的最小间隔（按本图配置） |
| **冷却时间(秒)** | 对局结束后地图进入冷却，期内无法在该图开新局；0 = 无冷却 |

### 3.5 补位设置

补位指：玩家使用**快速匹配**迟迟开不了局（超过"快速超时"秒数）时，中途加入正在进行中的**同模式**对局。

| 字段 | 说明 |
|---|---|
| **允许补位** | 总开关。关闭后该图对局不接受任何补位，补位模式不生效 |
| **补位模式** | `CONDITIONAL`：各队只补到**最低人数**（人够就不补），适合维持小局节奏<br>`ALWAYS`：各队补到**满员**（最高人数；0=无上限时把所有等待补位的玩家全部补入），适合大图保人 |

> 补位遵守队伍平衡：两队都能补时优先补人少的一队。

### 3.6 出生点与边界

- **红/蓝队出生点**：每队可配置多个出生点（X / Y / Z 三个输入框，支持负坐标），玩家重生时随机选择一个（5 秒内不重复）。**每队至少 1 个，否则拒绝保存**。
- **边界**：Min / Max X-Y-Z 六个值围成的区域（**六个值都必须正确填写，含 Y**——任一维留 0 会使该维检测范围变成 [0,0]），出界开始警告计时。判定规则：
  - **按玩家所站方块判定**（坐标向下取整），站在边界方块上不算出界；
  - **某维 Min 大于 Max 时自动交换**（填反不会恒判出界）；
  - 六个值全部为 0 表示完全未配置，不做越界检测。
- **对局维度**：对局所在的维度 ID（如 `minecraft:overworld` / `minecraft:the_nether`），非法值回退主世界。出生点与边界均在同一维度内生效。

### 3.7 卡片背景图

`背景图 Base64`：PNG 图片的 Base64 字符串（建议 512×288 横图），显示为玩家匹配菜单卡片背景（等比裁剪铺满 + 深色遮罩）。留空则显示默认深色底。快速匹配入口使用内置图，无需配置。

---

## 4. 商店物品配置

商店按**地图**独立配置：玩家只有在对局中打开商店（' 键）时，加载所在地图的商品。

| 列 | 说明 |
|---|---|
| **物品ID（可带NBT）** | 格式 `minecraft:cooked_beef` 或带数量 `minecraft:cooked_beef 64` |
| **价格** | 购买所需价格 |
| **限购次数** | 每名玩家每场对局的购买上限 |

快捷操作：手持物品点击"添加武器 (手持物品)"，自动把手中物品 ID 加入商品列表（默认价格 0、限购 1，记得改）。

> 仅**竞技模式**对局中可购买；休闲模式与对局外不可用。

---

## 5. 全局配置

| 字段 | 说明 |
|---|---|
| **快速超时(秒)** | 快速匹配队列等待超过该秒数后进入补位流程 |
| **默认装备** | 竞技模式开局发放的装备（清空背包后发放）。槽位 ID 参考 `/item replace entity` 的 `<slot>` 参数，当前允许：`armor.head`/`armor.chest`/`armor.legs`/`armor.feet`/`armor.body`、`container.0`~`container.35`；其余槽位（如 `weapon.mainhand`）保存时会弹窗报错 |

---

## 6. 配置文件直接编辑

配置位于服务端 `config/cstmm/configs/`，也可手动编辑 JSON 后执行 `/cstmm reload` 生效（OP≥2）。

- `maps.json`：地图配置数组
- `global.json`：全局配置

> 手动编辑前**先停服或小心操作**：配置界面保存会整体覆盖服务端文件。文件损坏时管理器不会覆盖用户文件，但会跳过坏数据并记录 WARN 日志。

`maps.json` 字段速查：

| JSON 键 | 对应界面字段 | 默认值 |
|---|---|---|
| `id` | 地图 ID | 必填 |
| `displayName` | 显示名称 | "" |
| `enabled` | 启用/禁用 | true |
| `winCondition` | 结算方式 | KILLS |
| `targetKills` | 目标击杀数 | 10 |
| `maxDuration` | 最大时长(秒) | 1800 |
| `tieRule` | 平局规则 | OVERTIME |
| `minRedPlayers` / `minBluePlayers` | 红/蓝队最低人数 | 1 / 1 |
| `maxRedPlayers` / `maxBluePlayers` | 红/蓝队最高人数 | 0 / 0 |
| `prepareTime` | 准备时间 | 5 |
| `boundaryWarningTime` | 边界警告 | 10 |
| `boundaryPenaltyKills` | 边界惩罚 | 5 |
| `kickCooldownSeconds` | 踢人冷却 | 60 |
| `cooldownSeconds` | 冷却时间 | 0 |
| `reinforceable` | 允许补位 | false |
| `reinforcementMode` | 补位模式 | CONDITIONAL |
| `dimension` | 对局维度 | minecraft:overworld |
| `boundary` | 边界 | 全 0（不检测） |
| `redSpawns` / `blueSpawns` | 出生点 `{"x":..,"y":..,"z":..}` | [] |
| `shopItems` | 商店 `{"itemId","price","maxPurchase"}` | [] |
| `backgroundBase64` | 背景图 Base64 | "" |

---

## 7. 典型配置示例

**4v4 竞技小图（街机节奏）**

- 结算方式 KILLS / 目标击杀数 30，红蓝最低 4 / 4，最高 4 / 4
- 允许补位：是，补位模式 CONDITIONAL（人不满时快速匹配可补齐开局）
- 冷却时间 10

**大图混战（人越多越好）**

- 结算方式 TIMER / 最大时长 1200，红蓝最低 6 / 6，最高 0 / 0（无上限）
- 允许补位：是，补位模式 ALWAYS（持续补人直到服务器满员）
- 冷却时间 30

**固定人数训练图（拒绝补位）**

- 结算方式 KILLS / 目标击杀数 10，红蓝最低 2 / 2，最高 2 / 2
- 允许补位：否
- 冷却时间 0

---

## 8. 注意事项

- **保存覆盖**：配置界面保存的是**全部地图**的整体配置，多 OP 同时编辑配置会互相覆盖，建议约定单人管理。
- **活跃对局保护**：正在进行对局的地图禁止删除/修改，保存会被服务端拒绝并弹窗。
- **出生点必填**：任一队出生点为空的地图无法保存。
- **ID 唯一**：重复 ID 的地图服务端会按 ID 覆盖，保存前界面会拦截。
- **原子写盘**：配置写盘为原子操作，意外断电不会产生半写损坏文件。
- **版本同步**：服务端与客户端模组必须同版本，否则玩家无法使用任何功能。
